"""Trading Strategy Engine.

Top-level package for the reconstructed TradingView strategy's
implementation. See ``docs/architecture/`` (repository root) for the
approved architecture this package implements, and
``docs/TRADINGVIEW_STRATEGY_BIBLE.md`` / ``docs/RULE_INDEX.md`` for the
business rules being reconstructed.

Milestone 4.1 scope: this package currently contains only the
``domain`` subpackage (structural domain models, no behaviour). See
``docs/architecture/IMPLEMENTATION_ROADMAP.md`` for what follows.
"""

__version__ = "0.1.0"
