# Architecture Freeze Checklist

Milestone 6.5. Each row verified against actual current code in `trading_engine/` as part of this milestone's review (see `FRAMEWORK_BASELINE_REPORT.md` for the full evidence trail). "Frozen: Yes" means this subsystem's shape/contract is stable and should not be redesigned going forward without new evidence forcing it. "Frozen: No" is reserved for genuine engineering rework needs unrelated to business mathematics.

| Subsystem | Status | Frozen | Requires Evidence | Future Extension |
|---|---|---|---|---|
| Domain models | Complete: 11 value-object/entity types, immutable dataclasses, structural-only validation, every business-behavior gap marked with a `# TODO (<RULE-ID>)` | **Yes** | Yes — most types (`Opponent`, `Premium`, `Strike`, `TrendPoint`, `SessionState`, `DomainEvent`) carry unresolved TODOs that need trading-mathematics evidence (OPPONENT-001/002/003, REVERSAL-001, STRIKE-001, TREND-001/002, SM-004, session lifecycle) before behavior can be added; the *shapes* themselves are stable | Once evidence lands, behavior methods (e.g. a "defeat" evaluator, a strike-selection function) get added to existing types — no redesign of the types expected |
| Rule framework | Complete: `Rule` protocol + `AbstractRule` base, `RuleExecutionContext`, `RuleOutcome`/exception hierarchy, `RuleCategory` | **Yes** | Yes — no rule in `docs/RULE_INDEX.md` has an implemented evaluation function yet; the contract is exercised only by test doubles | First real `Rule` implementation once any rule's mathematics is Known; also the mutate-vs-return Session-State-update mechanism question (`rules/context.py:71`), deferred pending the evidence that would make it concrete |
| Calculator framework | Complete: `Calculator` protocol + `AbstractCalculator` base, `CalculationContext`/`CalculationResult`, `CalculatorRegistry`, 6 concrete placeholder calculators (Strike, Trend, Opponent, Reversal, Edge, Weekly Future) all verified to raise `NotImplementedError` unconditionally | **Yes** | Yes — every calculator's `calculate()` body is the direct evidence gap; framework itself needs no further engineering | First real `calculate()` body once a rule's formula is Known — most likely `WeeklyFutureCalculator`/`StrikeCalculator` given the Task 5 recommendation |
| Registry (rule + calculator) | Complete: `RuleRegistry` with full Kahn's-algorithm topological sort, deterministic tie-breaking, cycle detection (`CircularDependencyError`), malformed/missing-dependency validation (`UnresolvedDependencyError`); `CalculatorRegistry` for calculator registration/lookup | **Yes** | No | Automated cross-check of `rules/dependencies.py` against `docs/RULE_INDEX.md`'s ledger (currently manual, drift risk) — a SAFE mechanical improvement if pursued, not evidence-gated |
| Dependency engine | Complete: static `RULE_DEPENDENCIES` table (transcribed from the 8 confirmed Rule IDs' "Depends On" column), `depends_on()`/`is_well_formed_rule_id()` helpers, diagnostics events on resolution/missing-dependency | **Yes** | Yes, indirectly — the table's completeness is bounded by which rules are confirmed; the intentionally-excluded Weekly Future → STRIKE-001 edge (undocumented in the ledger) is a known, deliberate limitation, not a defect | `Rule.depends_on()` as a protocol method (currently only the registry's external table knows dependencies) — SAFE mechanical addition, independent of evidence |
| Strategy engine | Complete: `StrategyEngine.run()` orchestrates rule/calculator execution via the pipeline and registry | **Yes** | Yes — "resulting Session State updates" (pipeline step 5) is unimplemented because no rule defines what such an update looks like (`strategy_engine.py:147`) | Session-State-update application, once both the update mechanism (deferred, see rules/context.py row above) and a rule's actual state-changing behavior are evidenced |
| Execution pipeline | Complete: `ExecutionPipeline` handles iteration, `maximum_rule_count`, `dry_run`, fail-fast/continue-on-error combinations, fatal-vs-unexpected exception handling, diagnostics emission gating via `EngineConfiguration.logging_enabled` | **Yes** | No — this subsystem's own logic is fully implemented and tested; it does not itself need trading evidence to function | How `ExecutionReport` should feed a future `Decision Object` is unspecified by `RULE_ENGINE_ARCHITECTURE.md` (`execution_report.py:76`) — deferred architecture question, to be resolved once Decision Objects gain real content |
| Diagnostics | Complete: 8 structured event types (`events.py`), `DiagnosticsSink` protocol with 3 implementations (no-op, ordered-collecting, stdlib-logging), verified zero dependency on `rules/`/`engine/`/`calculators/` | **Yes** | No | 4 open extension points from `EXECUTION_LOGGING_REPORT.md` — bounded/rotating sink, `CompositeDiagnosticsSink`, `ReplayDiagnosticsSink`, structured/JSON-lines log formatting — all DEFERRED to the Replay Engine milestone (4.5), not evidence-gated |
| Test architecture | Complete: pytest suite, one test file per source module, `conftest.py` fixtures/test doubles per subpackage, exhaustive-per-invariant `class Test<Concern>` organization, explicit "assert no business math happened" guard tests for calculators; 543 tests passing, 100% coverage, mypy/ruff/black clean (per orchestrating process) | **Yes** | No | Grows in lockstep with each future subsystem change; no structural rework anticipated |

**Overall: 9 of 9 subsystems Frozen = Yes.** No subsystem was found to need engineering rework unrelated to business mathematics. Every "Requires Evidence: Yes" row traces back to the same root cause documented in `FRAMEWORK_BASELINE_REPORT.md` Section 7 — Weekly Future High/Low arithmetic and the STRIKE-001 chain it feeds — rather than to any distinct per-subsystem gap.
